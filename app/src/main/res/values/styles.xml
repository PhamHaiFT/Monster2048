<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.MySplashNew" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">#FFF</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app_no_bg</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        # Required for
        # animated icons

        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/Theme.MyApp</item>
    </style>

    <style name="Theme.MyApp" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/textScoreColor
        </item> <!-- If it's a @drawable make sure it's in .xml format-->
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app_no_bg</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApp
        </item> <!-- This will switch back to your given theme once the splash screen is gone-->
    </style>
</resources>
